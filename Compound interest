#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    double P,R,CI;
    int T;
    scanf("%lf%lf%d",&P,&R,&T);
    CI=P * (pow((1 + R/100),T)- 1);
    printf("%.2lf",CI); 
    return 0;
}
